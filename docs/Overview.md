# 项目说明

NovelstudioAI 是一个面向网络小说创作的前端原型，核心理念：

- 章节树管理（类似 VSCode 资源管理器）
- Notion 风格块编辑
- 多 Provider + Agent 编排

## 目录说明

- `apps/web/`：前端应用（React + Vite + BlockNote）
- `apps/server/`：本地后端（Node + SQLite）
- `apps/server/data/`：SQLite 数据库文件（不提交）
- `docs/`：需求、规划、实现 TODO 文档
- `public/`：静态资源（仅前端使用）
